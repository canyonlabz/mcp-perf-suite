# perfpilot-mcp-perfanalysis — standalone spin-up
# Copy this file to `.env` if you want to override any defaults.
# SLA / analysis config is baked into the image.

DEPLOYMENT_MODE=local
MCP_TRANSPORT=http
MCP_HTTP_PREFIX=/perfpilot-mcp-perfanalysis
HTTP_PORT=8113
