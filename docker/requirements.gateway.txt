# PerfPilot Hub — Consolidated Python Dependencies for Docker
#
# This file combines dependencies from all 7 Docker-eligible MCP servers.
# Version pins use the highest minimum version across all servers to avoid conflicts.
#
# Source audit date: 2026-06-03
# Servers included: BlazeMeter, Datadog, JMeter, PerfAnalysis, PerfReport, Confluence, PerfMemory

# =============================================================================
# Core (shared by all servers)
# =============================================================================
fastmcp>=3.3.1,<4
httpx>=0.28.1
python-dotenv>=1.2.1
PyYAML>=6.0.2

# Starlette — pinned for CVE-2026-48710 (BadHost) security fix.
# FastMCP depends on Starlette transitively, but we pin explicitly to ensure
# the patched version is always installed in the Docker image.
starlette>=1.2.1

# =============================================================================
# PerfAnalysis + PerfReport (data science / charting)
# =============================================================================
pandas>=2.0.0
numpy>=2.3.5
scipy>=1.10.0
matplotlib>=3.8.0
seaborn>=0.13.2
pytz>=2024.1
aiofiles>=23.0.0

# =============================================================================
# PerfReport (document generation)
# =============================================================================
pypandoc>=1.13
pyandoc>=0.2.0

# =============================================================================
# Confluence (HTML/Markdown processing)
# =============================================================================
markdown2>=2.4.10
beautifulsoup4>=4.12.2
lxml>=5.1.0

# =============================================================================
# PerfMemory (PostgreSQL + vector embeddings)
# =============================================================================
psycopg2-binary>=2.9.11
pgvector>=0.4.2
openai>=2.30.0

# =============================================================================
# JMeter (test data generation — optional dep included for Docker)
# =============================================================================
faker>=33.0.0
