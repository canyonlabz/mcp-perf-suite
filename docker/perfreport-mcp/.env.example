# perfpilot-mcp-perfreport — standalone spin-up
# Copy this file to `.env` if you want to override any defaults.
# Chart / report / color configs are baked into the image.

DEPLOYMENT_MODE=local
MCP_TRANSPORT=http
MCP_HTTP_PREFIX=/perfpilot-mcp-perfreport
HTTP_PORT=8114
