# perfpilot-mcp-datadog — standalone spin-up
# Copy this file to `.env` and fill in the Datadog credentials.

DEPLOYMENT_MODE=local
MCP_TRANSPORT=http
MCP_HTTP_PREFIX=/perfpilot-mcp-datadog
HTTP_PORT=8111

DD_API_KEY=
DD_APP_KEY=
DD_API_BASE_URL=https://api.datadoghq.com
