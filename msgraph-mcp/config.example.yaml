server:
  name: "msgraph-mcp"
  version: "0.1.0-dev"
  description: "Microsoft Graph MCP Server for SharePoint and Teams integration (concept phase)"
  build:
    date: "2026-01-23"

general:
  enable_debug: False   # Enable/disable debug mode
  enable_logging: True  # Enable/disable logging

artifacts:
  # Dynamically resolved to {repo_root}/artifacts when left empty.
  # Set an explicit absolute path here only if you need a custom location.
  artifacts_path: ""

msgraph:
  ssl_verification: "disabled"     # Options: "ca_bundle" (use certs from env vars) or "disabled" (skip verification)
  time_zone: "America/New_York"    # Set the time zone

sharepoint:
  site_id: "<site-id>"
  drive_id: "<document-library-id>"
  include_subfolders: ["report", "analysis", "charts"]
  include_extensions: [".md", ".csv", ".png", ".json"]

teams:
  team_id: "<team-id>"
  channel_id: "<channel-id>"
  notify_on_start: true
  notify_on_complete: true
  notify_on_failure: true

