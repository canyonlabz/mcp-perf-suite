---
description: JMeter Correlation Naming Rules
alwaysApply: false
---
# JMeter Correlation Naming Rules

This workflow has been migrated to a **Cursor Skill** for reduced context overhead.

**Skill location:** `.cursor/skills/jmeter-correlation-naming/SKILL.md`

When this rule is referenced by other rules (e.g., playwright-browser-automation,
jmeter-hitl-editing, har-to-jmeter-jmx, swagger-to-jmeter-jmx), read and follow
the skill at the path above.

Key points:
- `correlation_naming.json` is **auto-generated** by `analyze_network_traffic` (Phase 4)
- Custom naming mappings are configured in `jmeter-mcp/correlation_config.yaml`
- The skill covers both reviewing auto-generated output and generating from scratch
- Full naming conventions and schemas are in `.cursor/skills/jmeter-correlation-naming/naming-conventions.md`
